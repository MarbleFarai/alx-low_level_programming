#include <stdio.h>
#include <time.h>
#include <stdlib.h>

/**
*main -> assign a random numner to the variable each time it is executed
*and print the last digit of the number stored in the variable n
*Return : always 0
*/
int main(void)
{
	int ch;
	for (ch = 97; ch = 127; ch++)
	{
		putchar(ch);
	}
	putchar(10); /* this is an ascii code for new line*/

	return (0);
}
